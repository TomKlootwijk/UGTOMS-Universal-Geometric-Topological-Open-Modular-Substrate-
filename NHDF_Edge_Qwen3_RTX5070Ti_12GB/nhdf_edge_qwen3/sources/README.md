# Source-material policy

The formal specification is included because it is the direct design basis for
this implementation. The earlier 30-page prompt-chain transcript was reviewed
for traceability but is not duplicated in the deliverable because it contains
personal identifiers and superseded, unvalidated hardware claims. Its SHA-256
is recorded in `SOURCE_SHA256.txt` so the user's original can be matched without
redistributing it.
